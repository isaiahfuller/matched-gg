/**
 * Represents the categories of a game.
 */
export enum GameCategory {
  bundle = 3,
  dlc_addon = 1,
  episode = 6,
  expanded_game = 10,
  expansion = 2,
  fork = 12,
  main_game = 0,
  mod = 5,
  pack = 13,
  port = 11,
  remake = 8,
  remaster = 9,
  season = 7,
  standalone_expansion = 4,
  update = 14,
}
