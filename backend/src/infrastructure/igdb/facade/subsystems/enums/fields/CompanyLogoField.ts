/**
 * Enum representing the available fields for an artwork in the IGDB API.
 */
export enum CompanyLogoField {
  alpha_channel = 'alpha_channel',
  animated = 'animated',
  checksum = 'checksum',
  height = 'height',
  id = 'id',
  image_id = 'image_id',
  url = 'url',
  width = 'width',
}

export enum ExpandedCompanyLogoField {
  alpha_channel = 'alpha_channel',
  animated = 'animated',
  checksum = 'checksum',
  height = 'height',
  id = 'id',
  image_id = 'image_id',
  url = 'url',
  width = 'width',
}
