/**
 * Enum representing the status of a game.
 */
export enum GameStatus {
  alpha = 2,
  beta = 3,
  cancelled = 6,
  delisted = 8,
  early_access = 4,
  offline = 5,
  released = 0,
  rumored = 7,
}
